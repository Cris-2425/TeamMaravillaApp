package com.example.teammaravillaapp.model

/**
 * Estilos de visualización posibles para una lista.
 *
 * Aún no se aplican en todas las pantallas, pero ya quedan
 * definidos para futuras vistas/ajustes de usuario.
 */
enum class ListStyle {
    LISTA,
    MOSAIC,
    ETC
}